language: python

services:
  - docker

before_install:
- docker-compose -f local.yml run app /bin/bash -c "/usr/src/app/entrypoint"

script:
- docker-compose -f local.yml run --rm app /usr/src/app/manage.py test --settings=config.settings.test "$@"