language: python
python:
  - "3.6"

services:
  - docker

before_install:
- docker-compose -f local.yml run app /bin/bash -c "./entrypoint"

script:
- docker-compose -f local.yml run --rm app ./manage.py test --settings=config.settings.test "$@"